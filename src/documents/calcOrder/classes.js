
export const exclude = [/*'doc.calcOrder'*/];

export function classes({doc, classes, symbols}, exclude)  {
  const {DocCalcOrder: DocObj} = classes;
  const {get, set} = symbols;

  // class DocCalcOrder extends DocObj {
  //
  // }
  // classes.DocCalcOrder = DocCalcOrder;
}
