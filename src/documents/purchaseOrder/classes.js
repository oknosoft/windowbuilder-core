
export const exclude = [/*'doc.purchaseOrder'*/];

export function classes({doc, classes, symbols}, exclude)  {
  const {DocPurchaseOrder: DocObj} = classes;
  const {get, set} = symbols;

  // class DocPurchaseOrder extends DocObj {
  //
  // }
  // classes.DocPurchaseOrder = DocPurchaseOrder;
}
